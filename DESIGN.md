# Design: RealityCI — Runtime for Physical Procedures

Generated by /office-hours on 2026-05-03
Branch: main
Repo: github.com/adityasingh2400/warden
Status: APPROVED
Mode: Builder
Hackathon: Nozomio Hackathon, May 9 2026, EF office San Francisco
Theme: "Build the Future of AI Agents"

---

## Problem Statement

Code has CI/CD. Physical work does not. Factories, labs, warehouses, hospitals, and construction sites have written procedures — SOPs, manuals, checklists, safety rules, incident reports — but those documents are disconnected from what actually happens on the floor.

Workers skip steps. Use the wrong part. Miss safety checks. Repeat mistakes from old incidents. The company technically "knows" the right process, but no runtime checks whether reality followed it.

Reference data: Siemens 2024 estimates the world's 500 largest companies lose ~$1.4T/year to unplanned downtime (~11% of revenue). OSHA estimates lockout/tagout compliance prevents ~120 fatalities and ~50,000 injuries per year — a measure of how expensive procedural drift becomes when safety is involved.

## What Makes This Cool

The primitive isn't "AI camera that detects things." The primitive is **compile company knowledge into runtime tests, then verify reality against them with citation**. That inversion is what makes RealityCI different from every camera-as-agent demo done before.

The judge-friendly metaphor: **GitHub Actions for factories.** The project sentence: **RealityCI turns SOPs, manuals, and incident logs into executable visual tests, then uses live cameras to verify whether real-world work followed the company's truth.**

The "whoa" beat in the demo: a mentor drops a fresh incident JSON into a watched folder mid-demo, and within 10 seconds the test graph adds a new check. Nia's continuous monitoring is the mechanism, but the user sees procedural memory in motion.

## Constraints

- **6 days to ship** (May 3 → May 9 demo at EF office)
- 3-minute pitch window (estimate; confirm with hackathon org)
- Demo must run live with backup video on hidden hotkey
- **No specialty parts. Everything used in the live demo is from a desk drawer or a corner store** (cardboard box, packing paper, tape, label sticker, a small item that goes in the box). No mail-order kits, no specialty hardware.
- Required sponsor: Nia / Nozomio (host)
- Strong-fit sponsors: Convex, Vercel, Tensorlake
- Skip: Aside, World Labs (live), Reacher

## Premises (all approved by user)

1. Lead with "runtime tests from company knowledge"; demote "cameras" to input device.
2. Factory tabletop demo, with a 20-second "this also verifies AI agents" close — instead of switching to office/agent-trace as the primary domain.
3. Nia compiles AND live-updates the test graph (not just RAG over one PDF).
4. Quality is the lead demo; safety + downtime narrated in the close.
5. Real camera, narrow rehearsed scope, prerecorded backup ready on hotkey.

User addition: **Nia is central to the product, every Nia feature is exercised, and the team produces a separate "Nia improvement report" identifying what worked, what didn't, and concrete proposals to make Nia better.**

## Approaches Considered

### Approach A: Single-Loop Tabletop (minimum viable)
Tabletop assembly, 5 SOP steps, finetuned YOLO + GPT-4V fallback. Nia indexes 3 static PDFs. Convex live dashboard. Vercel frontend. Skip Tensorlake/Hyperspell/Aside/World Labs.

- Effort: M
- Risk: Low
- Pros: Predictable demo. Hits all five premises.
- Cons: Nia is shallow (3 static PDFs is just RAG). Doesn't earn "this product depends on Nia." No improvement-report deliverable. No live-update story. Wins reliability, doesn't win the prize.

### Approach B: Nia-Centered RealityCI (chosen)
See Recommended Approach below.

### Approach C: Procedural Verification Primitive (creative / lateral)
Tabletop is the 60-second cold open, then demo pivots to "same primitive verifies AI agents" with a customer support agent trace example. Two runtimes, one product.

- Effort: L+
- Risk: High
- Pros: Maximally on-thesis ("Future of AI Agents"). Genuinely unique angle.
- Cons: Two demos = two failure surfaces. Splits attention in a 3-minute pitch. Agent-trace ingestion is its own 2-day side project. We capture 80% of the upside in B by closing with the agent-trace slide.

## Recommended Approach: B — Nia-Centered RealityCI

### Architecture

**Nia ingest sources — split into Core (must work for demo) and Stretch (added if hour-36 buffer permits):**

Core (3 sources, mandatory):
1. SOP PDF (procedure of record)
2. Equipment manual PDF (parts, tolerances, fastener torque)
3. Incident log JSONL — **monitored folder, live-updating during demo**

Stretch (3 sources, prize-aligned but cuttable):
4. Training video MP4 (transcript + frame timestamps)
5. Mock Slack channel export (operator notes, e.g., "yesterday we saw cap misalignment again")
6. Safety memo Drive folder (compliance reference docs)

If Core is wobbly on Day 4, Stretch sources DO NOT get added. Better to demo 3 sources flawlessly than 6 sources with one breaking.

**The compile step:**
The compiler is a deterministic local Python/TypeScript script that reads SOP PDF + manual + incident history (via Nia retrievals) and emits a test graph JSON. The graph contains ordered step nodes, branch nodes for safety checks, and reference edges to manual/incident citations.

**Tensorlake is a Stretch wrapper, not a Core dependency.** The same compiler script runs locally by default. If Tensorlake access is open by Day 3 and the team has buffer, wrap the local script in a Tensorlake sandboxed function for the prize-alignment story. If Tensorlake breaks at any point, fall back to the local version with no change to the demo flow.

**Tensorlake hard cut decision: 6pm Day 5 (May 7), Backend lead decides.** If the Tensorlake integration is not running end-to-end by 6pm Day 5, drop it and document the integration friction in `NIA_IMPROVEMENTS.md` (Tensorlake is also a sponsor — friction notes there are useful). After 6pm Day 5, no further Tensorlake work happens. Single decision-maker, single deadline. No drift.

This resolves the apparent contradiction with Premise 3: Nia is the **knowledge layer** the compiler reads from (continuously, with citations). Tensorlake is the optional **execution sandbox** for the compiler script. Both can be true; only the Nia layer is required.

**The runtime loop:**
1. Camera streams frames (1 fps is enough; do not chase real-time on the demo machine).
2. A small object detector (finetuned YOLOv8 on the 5 demo objects: red block, blue cap, screw driver, label, output bin) emits a structured event stream: `{object_present, hand_position, action_inferred}`.
3. A GPT-4V or Gemini Vision call runs every N frames as a fallback / scene reasoner: "what step does this look like?"
4. The verifier checks the current detected action against the expected node in the test graph.
5. On deviation: Nia retrieves the violated rule (paragraph-level citation) AND the prior incident pattern. The UI flashes red, shows citation + incident, generates incident report.
6. Convex propagates state changes to all connected clients in real time.

### Run lifecycle

Runs are started **manually via hotkey** by the demo operator. The hotkey creates a new `Run` in Convex with `started_by: 'manual_hotkey'`, the right `graph_id` and `detector` for the current scene, and state `AWAITING_1`. The run auto-ends when the state machine reaches `COMPLETED`, `DEVIATION` (after the citation surface fires), or 30s of `LOST_SIGNAL`. Manual is the right call for a scripted hackathon demo — auto-detect adds risk for a feature judges won't notice was missing. Auto-detect can be added post-hackathon (one of the listed "Open Questions" for v2).

### The Demo Has Two Camera Sources, Not One

This is the structural change that makes the demo land at scale and stay buildable in 6 days.

**Camera 1 — Live tabletop ("RealityCI in development"):**
A 5-step everyday-shipping SOP. Real camera on a real desk, real human hands. Used for the visceral moment-to-moment beat (correct steps go green, skipped steps flash red, citations appear). Runs against everyday objects — no parts ordering required.

The 5-step SOP (everyday shipping / fulfillment):
1. Place cardboard box on table
2. Add product (a book, mug, or any small object on hand)
3. Add packing material (crumpled paper, bubble wrap, or tissue)
4. Apply packing tape across the seal
5. Apply shipping label / sticker on the top face

This is the procedure judges instantly understand ("this is how Amazon packs every box"). The skipped-step failure has a real-world cost story: damaged-in-transit packages cost retailers billions per year, and 80% of those failures trace back to procedural drift (skipped packing material, wrong label placement, broken seal).

**Camera 2 — Pre-recorded production footage ("RealityCI at scale"):**
A 30-60s clip of an actual manufacturing line, warehouse, or pharma packaging plant — sourced from Creative Commons / stock footage / publicly licensed factory tour video. The footage plays back through the same YOLO + Gemini + verifier + Nia pipeline as the live camera. The judges see citations stream in real time against real industrial footage.

This solves the "ok but does it work at scale?" question without requiring a site visit. The pre-recorded video lets the team script a known step-skip moment at a known timestamp — the failure beat is rehearsable to the second.

Implementation note: the video plays through OpenCV / browser, frames go through the same detection pipeline as the live camera (the system does not know or care that this is recorded vs. live). Nia is genuinely live during playback — it queries the same indexed corpus as the live demo, so the citations are real, not faked.

**The live-ingest demo moment (deterministic version):**

The naive version chains three unverified systems (filesystem watcher, Nia continuous-monitor latency, Convex propagation) in a 10-second window in front of judges. That is the riskiest beat in the demo.

Make it deterministic instead:
- Pre-stage the incident JSON file in a separate folder. The "drop" is the demo team copying it into the watched folder via a single keypress macro.
- Cache the embedding for that exact file in advance. The Nia ingest path is exercised end-to-end during dress rehearsal so the only variable is the file move.
- The Convex update is bound to a specific `incidents.added` event, not to a polling loop. This eliminates polling-interval race conditions.
- A 10-second visible "Nia is ingesting..." spinner gives the team a believable cover even if the actual work takes longer. If the spinner finishes in 2 seconds, that's a positive surprise, not a problem.
- **Hard fallback:** if the live-ingest fails for any reason, the operator hits a hidden hotkey that injects the new test-graph node directly via a Convex mutation, with the same visual outcome. Judges cannot tell the difference, and the demo continues.

The product story is unchanged ("Nia ingested a new incident; the test graph adapted"). The execution is hardened so the story always lands.

**The Nia Improvement Report (separate deliverable, ~2 pages):**
Document 3-5 friction points encountered during build. Likely candidates worth investigating during the build (do not pre-commit findings — the report's value is honesty about what the team actually hit):
- PDF table extraction and step-ordering preservation when SOPs use numbered tables
- Video transcript citation grounding at the frame level (not just text-level)
- Structured queries against JSONL incident logs (typed predicates vs. semantic search)
- Continuous-monitor ingest latency under N=6 sources
- Citation provenance precision (paragraph vs. document level)

For each friction point: what we tried, what worked around it, and a concrete proposal for what Nia could do natively.

### The Magical Loop (180-second pitch — adjust if pitch length differs)

1. **0-15s:** "Code has CI/CD. Physical work doesn't. We built it." Show the test graph for the shipping SOP. Five nodes, all gray. Live tabletop camera visible.
2. **15-50s — Live tabletop, correct path:** Demoer packs the box correctly through step 3. Each test graph node turns green as it's verified. Citation surface shows which rule just passed, with the source PDF thumbnail visible.
3. **50-90s — Live tabletop, failure path:** Demoer applies the label *before* sealing the box (or skips packing material). **UI flashes red.** Citation appears: "Shipping SOP §2.3, page 2, paragraph 1 — verbatim." Prior incident surfaces: "Pattern matches incident #23, Oct 12 — caused $1,200 in damaged-in-transit returns." Convex propagates state across all dashboards instantly.
4. **90-130s — Production playback:** Cut to a 30-second clip of a real manufacturing/warehouse line. *"This is the same system, watching real production."* Citations stream as the footage plays. At a known timestamp (rehearsed), a procedural skip happens on screen. RealityCI flags it live, with the same Nia citation surface. Judges see scale fit instantly.
5. **130-150s — Live ingest beat:** Demo ops drops a new incident file via hotkey macro. Spinner: "Nia ingesting...". Within 10s, the test graph adds a new check node. Dashboard updates without reload. **The wow beat.** (Hidden hotkey fallback: direct Convex mutation produces same outcome if the live ingest hiccups.)
6. **150-170s — Closing slide:** "Same primitive runs against AI agent traces. Today: shipping floors and assembly lines. Tomorrow: agent governance." Mock screen of agent trace verification.
7. **170-180s — The numbers:** Cite Siemens $1.4T downtime + OSHA 120-fatalities-prevented. Hand off to questions.

### Sponsor Stack (in priority order)

- **Nia / Nozomio (mandatory):** indexing, retrieval, continuous monitoring, citation. The architecture depends on it. Improvement report directly addresses the host sponsor.
- **Convex:** real-time dashboard state. Propagates test graph updates, incident events, camera detections to all judges' screens instantly. Judges see Convex doing the thing it's designed for.
- **Vercel:** frontend deploy. Polish matters because this product wins through visual clarity.
- **Tensorlake:** SOP→test-graph compiler runs as a sandboxed Tensorlake job. Cuttable to a local script if day-3 burndown is bad, but worth fighting to keep — strong story, prize-aligned.
- **Hyperspell (stretch, only if hour 36 has buffer):** operator/station-level memory. "Operator A misses inspection step X 23% of the time — surface a personalized reminder when they begin the procedure."

Skip: Aside (browser action layer doesn't fit the runtime story), World Labs (cool but too far from core, only as pre-rendered training video if at all), Reacher (not relevant).

### Tech Stack

- **CV detection:** YOLOv8**n** (nano variant — smallest weights, fast CPU inference) finetuned on the 5 everyday objects: cardboard box, packing paper / bubble wrap, packing tape, shipping label, and the small product item (book or mug). Plan ~8 hours of labeling + training across Day 1 evening and Day 2. Use Roboflow for labeling speed. Augment with rotation/lighting variants; the venue lighting will not match the lab.
- **Inference location:** YOLO runs locally on the demo laptop. **No hosted inference.** EF venue wifi cannot be a hard demo dependency. Demo laptop runs at 1 fps, so even ~50ms-per-frame YOLOv8n CPU inference has 95% headroom.
- **Production-footage detection:** Same YOLO model is *not* expected to generalize to real factory footage. For the production-playback segment, use Gemini 2.0 Flash directly on sampled frames — the VLM handles the variety of objects in real industrial footage well enough for "what step is this?" reasoning. The CV pipeline switches to VLM-only mode when the input source is the playback file (one config flag).
- **VLM scene reasoner:** **Gemini 2.0 Flash** (decision: lower latency than GPT-4V, sufficient quality for scene-level "what step is this?" reasoning). Runs every 1 second on a sampled frame. Caches the last 3 scene labels to avoid flicker.

  **Latency discipline:** YOLO is the primary detection signal; Gemini is *additive context*, never blocking. The detection pipeline waits at most 800ms for a Gemini response — beyond that, the in-flight call is discarded and the YOLO-only event is emitted. The verifier never blocks on the VLM. Latency budget: detection event from camera frame to UI render is <200ms (YOLO ~50ms + state machine ~1ms + Convex propagation ~100ms + citation lookup ~10ms + render ~16ms).
- **Step-skip detection logic (state machine, not prose):**

```
              detection event       no detection (5s)
                │                   │
                ▼                   ▼
  ┌──────────┐  start_run   ┌──────────────┐  out-of-order  ┌─────────────┐
  │  IDLE    ├─────────────▶│  AWAITING_N  ├───────────────▶│  DEVIATION  │
  └──────────┘              │              │  detection      └──────┬──────┘
        ▲                   └────┬─────────┘                        │
        │                        │                                  │ flag + cite
        │                        │ step_N detected                  │
        │                        ▼                                  │
        │                   ┌──────────────┐                        │
        │                   │ OBSERVING_N  │                        │
        │                   │ (2s confirm) │                        │
        │                   └────┬─────────┘                        │
        │                        │ confirmed (no contradict)         │
        │                        ▼                                  │
        │                   ┌──────────────┐                        │
        │                   │  STEP_N_DONE ├──N==final──┐           │
        │                   └────┬─────────┘            │           │
        │                        │ N=N+1                ▼           │
        │                        │              ┌──────────────┐    │
        │                        ▼              │  COMPLETED   │    │
        │                   AWAITING_N+1        └──────┬───────┘    │
        │                                              │            │
        │  reset                                       │ end_run    │
        │◀─────────────────────────────────────────────┘────────────┘
        │
        │  (lost signal recovery)
        │
   ┌────┴───────────┐  signal_returns + still in window
   │  LOST_SIGNAL   │◀──────────────from any waiting/observing state
   └────────────────┘
```

A step is "skipped" when in `AWAITING_N`, the detector emits an event consistent with step N+1, N+2, etc. (the "out-of-order" trigger). The 2s confirmation window inside `OBSERVING_N` avoids flickering on noisy detections. `LOST_SIGNAL` engages when no detection events arrive for 5s; if signal returns within 15s the run resumes, otherwise auto-reset to IDLE. Implementation: small TypeScript module (state machine + transition table + `currentState`, `currentStep`, `windowStart` fields), unit-tested before integration.
- **Backend:** Convex (real-time state, queries, auth, incident report storage). Schema:
  - `runs` — `{id, source: 'live_camera' | 'playback', graph_id, detector: 'yolo' | 'vlm_only', started_at, ended_at, state: 'IDLE' | 'AWAITING_N' | 'OBSERVING_N' | 'DEVIATION' | 'LOST_SIGNAL' | 'COMPLETED', current_step, started_by: 'manual_hotkey' | 'auto_detect'}`
  - `graphs` — compiled test graphs `{id, sop_doc_id, nodes: [{step_n, expected_action, citation_ref}], compiled_at}`
  - `detections` — `{run_id, frame_ts, objects, action_inferred, confidence}`
  - `citations` — `{run_id, rule_id, source_doc, page, paragraph, text, video_clip?: {start_s, end_s, thumbnail}, image_region?: {page, bbox, caption}}`
  - `incidents` — `{id, json_blob, ingested_at, matched_steps}` (live-monitored folder)
  - `reports` — `{run_id, generated_at, citation_chain, recommended_training}`

  **Why an explicit `runs` entity, not a config flag:** the verifier reads `Run.graph_id` to pick its test graph and `Run.detector` to pick its detection backend, instead of branching on `if source == 'playback'` in 6 places. Switching demos = create a new Run with different fields. A live-tabletop run and a playback run can co-exist without state collision (state isolated by `run_id`). Schema cost ~20 lines; saves the Day-4 firefight where one demo or the other doesn't switch cleanly.
- **Frontend:** Next.js on Vercel. Three-pane layout (live camera left, detected objects + test graph middle, citation + incident right). Bottom: incident report.
- **Knowledge layer:** Nia indexes 3 Core sources (with 3 Stretch deferred). Queries via Nia SDK at verification time. Citation surface returns paragraph-level provenance with source doc thumbnail.

  **Nia is the primary retrieval path, NOT just basic RAG.** Concretely, Nia is exercised for:
  - Continuous-monitor ingest of the live-incident folder (load-bearing for the wow beat)
  - Multi-source query at verification time ("did this violation happen before?" pulls across SOP + manual + incident log + Slack notes)
  - Cross-source citation chains (rule violated → manual section → similar prior incident)
  - Semantic similarity matching ("this looks like incident #23") — fuzzy beats exact-string matching for the "have we seen this before?" beat

  **Citation provenance fallback (Day 1 spike validates which path applies):**
  - Day 1 morning: pre-process the Shipping SOP PDF into a structured JSON: `{sections: [{id: '2.3', page: 2, paragraph: 1, text: '...'}]}`. ~2 hours of work; counts as primary if Nia paragraph-level retrieval is weak.
  - Day 1 afternoon: feed SOP to Nia, verify paragraph-level retrieval works for known sections.
  - **If Nia paragraph retrieval works:** Nia is the primary citation source. The structured JSON sits silent as a fallback.
  - **If Nia paragraph retrieval is document-level only:** verifier looks up by structured-JSON anchor for citation text/page/paragraph; Nia still owns continuous-monitor ingest, multi-source query, and incident pattern matching (its load-bearing roles). This becomes Nia Improvement Report finding #1 with a concrete proposal: "expose paragraph-level provenance in retrieval results."

### Multi-modal citation anchors (the differentiator)

Text quotes are table stakes. The thing that makes the citation surface *unmistakably more than RAG* is precise multi-modal anchors:

- **Video clip citations:** when a procedural rule is taught in a training video, the citation surface returns `{video_id, clip_start: '1:23', clip_end: '1:45', transcript: '...', frame_thumbnail: 'path/to/frame.jpg'}`. The UI plays the 22-second clip inline next to the violation. Implementation: pre-process the training video into chunks with whisper-transcribed segments + sampled keyframes; index each chunk in Nia with `{start_s, end_s, frame_path}` metadata; surface those fields in the citation card.
- **Photo / diagram region citations:** when an equipment manual contains diagrams (torque tables, fastener orientation diagrams, hazard pictograms), the citation surface returns `{page, bbox: [x1, y1, x2, y2], caption}`. The UI overlays the bounding box on the page screenshot. Implementation: pre-process diagrams using Gemini Vision to label regions; index each region in Nia with bbox metadata; surface those fields in the citation card.

**Why this matters for the demo:** the difference between "your manual said cap goes first" (text quote) and "your manual said cap goes first AND here's the 22-second training clip teaching that step AND here's the diagram on page 4 showing the assembly order" (multi-modal evidence package) is the difference between *RAG demo* and *new primitive*.

**Why this matters for the Nia Improvement Report:** Nia probably exposes some of this natively (video transcripts likely yes; bbox region anchors likely no). Whatever's missing becomes a concrete proposal: "video segment + frame metadata as first-class citation fields," "vision-region anchors with bbox metadata." We ship the version we wished Nia had, then we propose Nia ships it.

Either way, Nia is doing more than RAG.
- **Compiler:** Local TypeScript/Python script consumes Nia retrievals, emits test graph JSON. Optionally wrapped in a Tensorlake sandboxed function for the sponsor story (Stretch).
- **Camera input:** USB webcam, 1080p, fixed mount. **Bring your own ring light + diffuser to the EF office.** Venue lighting will be different from where you trained YOLO; do not rely on it.

### Network resilience — full offline-capable demo

Hard rule: **EF venue wifi cannot be a hard demo dependency.** Pre-cache everything the demo needs to the laptop on Day 5. The pipeline degrades gracefully when any cloud service is unreachable.

Pre-cached on demo laptop (Day 5 evening, ~3 hours of work):
- Compiled test graph for the Shipping SOP (JSON in local file)
- All citations the demo will encounter (text, video clips, image regions) — cached as static JSON keyed by rule_id
- 3 incident scenarios for the live-ingest beat (pre-staged JSON files + their pre-computed Nia matches)
- Production-line playback video file (local mp4)
- Local Convex dev instance running on demo laptop (Convex supports local dev) as fallback if cloud Convex is unreachable
- YOLOv8n weights file (already local — confirmed)
- Optional: a small local VLM (Llama 3.2 Vision via Ollama) as a Gemini-Flash fallback for the playback scene reasoner

Online services are still preferred (better latency, cleaner story), but the demo runs end-to-end with airplane-mode wifi. Test this on Day 6: turn wifi off and run the full demo. If anything breaks, fix it before the hackathon.

Citation latency budget: <200ms from "violation detected" to "citation card rendered." Online path uses Nia retrieval. Offline path uses local JSON lookup. Both feel instant.

## Open Questions

- **Team size and roles.** How many people on the team? At minimum the build needs: (1) CV/ML lead (YOLO + VLM), (2) Backend lead (Convex schema + Nia integration + compiler), (3) Frontend lead (Next.js + dashboard polish + camera UX), (4) Demo ops / pitch lead. One person can wear two hats; fewer than two people on a build this dense is risky.
- **Everyday object set sourced.** Cardboard box, packing paper, packing tape, shipping label / sticker, and one small product (book or mug). All from a desk drawer or corner store. Confirm on hand by Day 1 evening so labeling can start.
- **Production footage sourced (timeboxed Day 1 morning, 60 min, hard 11am cutoff).** A 30-60s clip of real factory / warehouse / pharma packaging work, Creative Commons or public stock. Sources to check: Pexels, Pixabay, YouTube factory tour B-roll (with citation), Storyblocks. Pre-process to find the timestamp where a procedural skip naturally occurs; if no natural skip exists, edit the clip to remove a step. **Fallback if no suitable clip found by 11am:** move the tabletop shipping kit to a different angle and lighting, shoot 30s of "miniature production line" footage, and use that as the playback source. The story still lands (same primitive, different scale).
- **Tensorlake access timing.** If sponsor access is gated to event day, the local compiler is your only path; Tensorlake becomes a Day-7 swap, not a Day-3 build. Verify access status on Day 1.
- **Nia continuous-monitor support.** Does the SDK support filesystem watching out of the box, or do you need a polling shim? If polling, document latency in the Nia Improvement Report.
- **Pitch length.** Brief assumes ~3 minutes; confirm with hackathon organizers. 5-minute pitch = add the agent-trace closing demo; 3-minute pitch = trim the warm-up and lead with the skipped-step beat.
- **Backup video ownership.** One person owns the prerecorded backup run. They record it on Day 5 evening, test the hotkey, and own the runbook for what happens if anything fails on stage.
- **SOP authoring.** The 5-step SOP itself needs to exist as a real PDF (with sections 2.1, 2.2, 2.3, etc. — the citation needs a real anchor). Day 1 morning task. ~30 min of work, but easy to forget.

## Success Criteria

The demo is a success if:

1. The test graph compiles from the SOP PDF in under 30 seconds during demo (Tensorlake or fallback).
2. The live tabletop camera detects all 5 everyday objects (box, paper, tape, label, product) correctly under venue lighting.
3. The skipped-step deviation flashes red with the verbatim Nia citation visible to judges.
4. The production-footage playback runs through the same pipeline and surfaces a citation at the rehearsed timestamp.
5. The live-ingest moment lands — new incident file → new test graph node within 10 seconds (or hidden-hotkey fallback fires invisibly).
6. The Convex dashboard updates without page reload throughout.
7. The Nia Improvement Report is delivered as `NIA_IMPROVEMENTS.md` in the submission repo.
8. Backup video is rehearsed and ready on a hidden hotkey.

Non-goals (boil the lake later, not this week):
- General industrial CV (only 5 specific objects need to work)
- Multi-camera support
- Authentication / multi-tenant
- Hyperspell unless hour 36 looks easy
- World Labs live generation
- Aside browser actions

## Distribution Plan

- **Hackathon submission:** GitHub repo, Vercel deploy URL, demo video, NIA_IMPROVEMENTS.md.
- **Devpost:** standard submission with screenshots + 3-min demo video.
- **Post-hackathon:** repo stays public. README documents the SOP→test-graph compiler so other Nozomio users can fork it. The Nia Improvement Report gets shared with Nozomio team as a PR comment or issue on their repo (if applicable) — judges remember teams who contribute back.

CI/CD: Vercel auto-deploys on main pushes. Convex deploys via `npx convex deploy`. Skip GitHub Actions; ship the Vercel + Convex defaults.

## Next Steps

**Treat the next 6 days as 6, not 7.** May 3 is half-gone (planning + setup); May 9 is hackathon. Plan compresses to May 3 evening through May 8.

**Cross-cutting (every day, not a single task):**
- Log Nia friction points in `NIA_IMPROVEMENTS.md` as you hit them. Drafting on Day 5 from memory loses 60% of the real findings. Two-line entries are fine; polish on Day 5.
- Daily 30-minute end-of-day burndown check. If a Core deliverable slips, cut a Stretch immediately — do not let scope debt compound.

**Day 1 (May 3, evening):**
- Lock team and roles (CV, Backend, Frontend, Demo ops).
- Confirm everyday object set: cardboard box, packing paper, tape, label, small product. All from a drawer / corner store.
- Source the production-line footage (30-60s clip, Creative Commons, with a natural or edited procedural skip).
- Author the 5-step Shipping SOP PDF with real section anchors (§2.1 through §2.5). ~30 min. Format matters — Nia retrieval needs paragraph anchors.
- Verify Nia SDK access + test paragraph-level retrieval against the SOP PDF.
- Verify Nia continuous folder monitoring (or determine polling fallback).
- Verify Tensorlake access. Plan accordingly: native (Stretch wrapper) vs. local-only.
- Initialize repos: Next.js + Convex + YOLO project skeleton in github.com/adityasingh2400/warden.

**Day 2 (May 4):**
- YOLO labeling marathon (Roboflow recommended). ~500 frames of the 5 everyday shipping objects, augmentation passes.
- YOLOv8n finetune pass 1. Validate on test set under varied lighting.
- Convex schema implemented per the design (runs, graphs, detections, citations, incidents, reports).
- First Vercel deploy with placeholder UI.
- Nia ingest pipeline working for the 3 Core sources end-to-end.
- Production-footage pipeline scaffolded: video plays back, frames feed into Gemini Flash VLM, output goes to the same Detector interface as YOLO.
- **Vitest set up. State machine module + 11 unit tests written and passing.** This is the highest-leverage test work in the project — the FSM is the single point of failure. Unit-test before integrating.

**Day 3 (May 5):**
- SOP→test-graph compiler implemented (local script). Test graph generation working from real SOP PDF.
- Gemini 2.0 Flash scene reasoner integrated. Latency profiled — confirm <1.5s per call.
- Citation resolver implemented (Nia primary, structured-JSON fallback). 5 unit tests with mocked Nia.
- SOP compiler tests (4 unit + 1 integration).
- Live verification loop end-to-end: camera → YOLO + Gemini → state machine → graph node update → Nia citation retrieval.
- First end-to-end dry run (no judging, just see if it works).

**Day 4 (May 6):**
- Convex real-time dashboard polish. Three-pane layout finalized.
- Citation surface: paragraph-level Nia retrieval + multi-modal anchors (video clip + image region) + source doc thumbnail.
- Live-ingest beat hardened: pre-staged JSON file, hotkey macro, cached embeddings, hidden hotkey fallback (direct Convex mutation).
- Live ingest watcher integration tests (2 tests).
- Playwright set up. Critical paths #1 (live tabletop happy) and #2 (skipped step beat) recorded as E2E specs.
- First *intentional rehearsal* (timed, in front of one teammate playing judge).
- **Cut decision:** if Core works, start Stretch (training video transcript with multi-modal citation). If Core is wobbly, freeze scope and polish.

**Day 5 (May 7):**
- Demo polish. Ring light setup. Camera mount. Recording station.
- Critical paths #3, #4, #5 recorded as Playwright E2E.
- Eval suite for Gemini Flash (10 known frames → expected step labels).
- **Network-resilience caching pass (~3 hours):** pre-cache compiled graph, all citations, 3 incident scenarios, playback video, local Convex dev fallback, optional local VLM. Goal: full demo runs offline.
- Backup video recorded by demo ops lead. Hidden hotkey tested 5+ times.
- **Tensorlake hard cut decision at 6pm:** if integration not running end-to-end, drop and document in `NIA_IMPROVEMENTS.md`.
- `NIA_IMPROVEMENTS.md` polished: 3-5 specific friction points with concrete proposals.
- Second timed rehearsal. Time the pitch end-to-end.

**Day 6 (May 8 — day before hackathon):**
- **Network-off rehearsal:** turn off wifi, run the full demo. Anything that breaks gets fixed before bed. This is the Day 6 acceptance test.
- Final rehearsal in lighting conditions as close to EF venue as possible.
- Pitch deck (3-5 slides max). Talking points memorized by pitch lead.
- Pack: laptop, camera, mount, ring light, diffuser, USB cables, power strip, kit pieces, backup laptop if available.
- Travel to EF office area. Sleep early.

**Day 7 (May 9 — hackathon):**
- Arrive 90 min before pitch slot. Setup: lighting, mount, camera angle.
- Run YOLO calibration pass on the actual lighting (5-min model swap if accuracy degrades).
- Demo. Incident report. Q&A.

## What I noticed about how you think

- You said "ensure Nia is crucial to the center of our product... ensure its features are being fully used... identify what nia does bad and how we can make that even better as a part of our hackathon." Most hackathon teams treat sponsors as integration boxes to check. You're treating the host sponsor as a partner whose product you can leave better than you found it. That's a separate prize lever most teams will never pull, and it tracks with how the people who win these things think.
- The brief drew a hard "Narrow Build Scope" boundary — "do not build the full industrial platform" — and listed exactly 10 steps for the loop. Discipline about what NOT to build is rare under hackathon time pressure. Most teams scope-creep on day 2.
- The deliberate separation of the 10-second pitch from the technical brief, plus pre-written sponsor justifications and a judge-friendly metaphor ("GitHub Actions for factories"), reads like someone who's pitched before and knows the work happens before the demo, not in front of it.
- You distinguished "Cameron detects events. RealityCI tests reality against company procedure" — naming the prior art and saying exactly how you're different in one line. That's the kind of crisp-positioning instinct that goes a long way past hackathons.
