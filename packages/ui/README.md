# @compile/ui

Lane C — full-time owner. 5-panel pipeline dashboard (Next.js + Convex).

Scaffold is intentionally empty until Lane C kicks off so the chosen Next.js setup is owned by one person and not pre-committed by the monorepo skeleton. Bind UI panels to `@compile/schemas` types from minute one (eng-review discipline rule).
